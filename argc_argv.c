#include <stdio.h>
/**
 * argc - argument count: counts the number of arguments into the command line when running a program
 * argv - argument vector: is an array that holds the string values of the strings that were entered in the Command line
 * char* - is an array of strings
 */

int main(int argc, char* argv[])

{
	printf("argc = %d\n", argc); //this prints the contents of argc
	return 0;
}
