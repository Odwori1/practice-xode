#include <stdio.h>
/**
 * Unary plus (+): It indicates the positive value of the operand.
Example: var x = +5; // x is assigned the value 5

Unary minus (-): It indicates the negative value of the operand.
Example: var x = -5; // x is assigned the value -5

Increment (++): It increases the value of the operand by 1.
Example: var x = 5; x++; // x is now 6

Decrement (--): It decreases the value of the operand by 1.
Example: var x = 5; x--; // x is now 4

Logical NOT (!): It returns the opposite of the Boolean value of the operand.
Example: var x = true; var y = !x; // y is assigned the value false

Bitwise NOT (~): It flips all the bits of the operand.
Example: var x = 5; var y = ~x; // y is assigned the value -6
*/
int main()

{
	int money = 45;
	int bill = 30;
	int total = money - bill;
	printf("my expense is:  %d\n", total);







return(0);
}
